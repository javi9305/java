
`````` java

 public static void main(String[] args) {
        //Inicializo el contador en cero.
       var contador = 0;
       //Haz primero lo que contiene el do y luego pasa al ciclo while.
       do{
           System.out.println("contador = " + contador);
           contador++; //Incrementa por cada iteraccion el contador.
           //contador = contador +1;
       }
       while(contador < 3); //Mientras contador sea menor que 3 repite el ciclo.
    }

    `````` 